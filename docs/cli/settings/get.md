<!-- @generated by usage-cli from usage spec -->
# `nise settings get`

- **Usage**: `nise settings get [-l --local] <SETTING>`
- **Source code**: [`src/cli/settings/get.rs`](https://github.com/jdx/mise/blob/main/src/cli/settings/get.rs)

Show a current setting

This is the contents of a single entry in ~/.config/mise/config.toml

Note that aliases are also stored in this file
but managed separately with `nise tool-alias get`

## Arguments

### `<SETTING>`

The setting to show

## Flags

### `-l --local`

Use the local config file instead of the global one

Examples:

```
$ nise settings get idiomatic_version_file
true
```
