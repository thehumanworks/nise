<!-- @generated by usage-cli from usage spec -->
# `nise generate github-action`

- **Usage**: `nise generate github-action [FLAGS]`
- **Source code**: [`src/cli/generate/github_action.rs`](https://github.com/jdx/mise/blob/main/src/cli/generate/github_action.rs)

Generate a GitHub Action workflow file

This command generates a GitHub Action workflow file that runs a nise task like `nise run ci`
when you push changes to your repository.

## Flags

### `-t --task <TASK>`

The task to run when the workflow is triggered

**Default:** `ci`

### `-w --write`

write to .github/workflows/$name.yml

### `--name <NAME>`

the name of the workflow to generate

**Default:** `ci`

Examples:

```
$ nise generate github-action --write --task=ci
$ git commit -m "feat: add new feature"
$ git push # runs `nise run ci` on GitHub
```
